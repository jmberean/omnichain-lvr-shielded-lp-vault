{
  "name": "lvr-shield-subgraph",
  "version": "0.1.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "create-local": "graph create --node http://localhost:8020 lvr-shield",
    "deploy-local": "graph deploy --node http://localhost:8020 --ipfs http://localhost:5001 lvr-shield"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.97.1",
    "@graphprotocol/graph-ts": "^0.34.0",
    "typescript": "^5.4.5"
  }
}
